
## Generating data frames for entities and attributes

The function `entities_with_attribute` allows to fetch entities with certain attributes, producing
a data frame where entities are rows and attributes are columns. Various parameters allow for
limiting attributes with specific types and values, aggregating several attributes in a single column
filtering by dates , kleio group name, and list of entity ids. 
## entities_with_attribute